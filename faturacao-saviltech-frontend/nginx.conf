server {
    listen 80;
    server_name localhost;
    root /usr/share/nginx/html;
    index index.html;

    # SPA (React Router) — a API é chamada em http://SEU_IP:3000 (VITE_API_URL no build)
    location / {
        try_files $uri $uri/ /index.html;
    }
}
