# URL do backend (sem /api no final). Em produção na VPS use o IP ou domínio público com :3000.
# Exemplo:
# VITE_API_URL=http://147.93.89.17:3000
#
# Em desenvolvimento, se não definir, o código usa http://localhost:4000 por defeito.
# VITE_API_URL=http://localhost:4000
